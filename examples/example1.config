def system = dict(
    hostname = @"server01",
    os = @"Linux",
    users = { @"root". @"admin". },
    services = dict(
        ssh = dict(
            enabled = True,
            port = 22
            ),
        web = dict(
            enabled = False
            )
        )
    );